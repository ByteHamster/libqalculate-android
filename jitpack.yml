jdk:
  - openjdk11
before_install:
   - echo "a"
   - apt install swig
   - sudo apt install swig
install:
   - echo "Running a custom install command"
   - ./gradlew install
env:
   MYVAR: "custom environment variable"
